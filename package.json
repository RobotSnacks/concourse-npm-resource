{
  "private": true,
  "name": "npm-concourse-resource",
  "scripts": {
    "build:image": "docker build --tag robotsnacks/concourse-npm-resource .",
    "test": "node -c *.js"
  },
  "dependencies": {
    "semver": "^5.5.0"
  }
}
